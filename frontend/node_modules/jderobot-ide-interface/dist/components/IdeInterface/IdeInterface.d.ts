import { CommsManager } from "jderobot-commsmanager";
import { Layout, ExplorerEntry, EditorsEntry, ViewersEntry } from "Types";
export interface IdeInterfaceStyles {
    bgColor?: string;
}
interface IdeInterfaceProps {
    commsManager: CommsManager | null;
    resetManager: Function;
    project: string;
    explorers: ExplorerEntry[];
    editorApi: any;
    extraEditors: EditorsEntry[];
    viewers: ViewersEntry[];
    layout: Layout;
    options: any;
    splashIcon?: JSX.Element;
}
declare const IdeInterface: ({ commsManager, resetManager, project, explorers, editorApi, extraEditors, viewers, layout, options, splashIcon, }: IdeInterfaceProps) => JSX.Element;
export default IdeInterface;
